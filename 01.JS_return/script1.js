// Poniższy kod robi to samo, co script.js, tylko zapis jest krótszy, bez zapisywania wszystkiego do zmiennych po drodze
function sum(number) {
  return number * number;
}

alert(sum(5));
alert(square(27));
alert(square(-15));

// Sukces wywołanej funkcji nie zależy od jej nazwy ale od działania, które dana funkcja ma wykonywać
